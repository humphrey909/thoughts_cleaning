<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Thoughts_cleaning"
        tools:targetApi="31">
        <activity
            android:name=".views.game.view.activity.container.GameActivity"
            android:exported="true"
            android:theme="@style/Theme.Thoughts_cleaning"
            />
        <activity
            android:name=".views.start.view.activity.container.StartActivity"
            android:exported="true"/>
        <activity
            android:name=".views.main.view.activity.container.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>