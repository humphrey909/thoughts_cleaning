<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Thoughts_cleaning"
        tools:targetApi="31" >

        <!-- <activity -->
        <!-- android:name=".views.record_problem.view.activity.container.RecordProblemActivity" -->
        <!-- android:exported="true" -->
        <!-- android:theme="@style/Theme.Thoughts_cleaning" -->
        <!-- /> -->
        <activity
            android:name=".views.game.view.activity.container.GameActivity"
            android:exported="true"
            android:theme="@style/Theme.Thoughts_cleaning" />
        <activity
            android:name=".views.start.view.activity.container.StartActivity"
            android:exported="true" >

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->

        </activity>
        <activity
            android:name=".views.record_problem.view.activity.container.RecordProblemActivity"
            android:exported="true"
            android:theme="@style/Theme.Thoughts_cleaning" >
        </activity>
        <activity
            android:name=".views.main.view.activity.container.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Thoughts_cleaning" >
        </activity>
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- 리다이렉트 URI: "kakao${NATIVE_APP_KEY}://oauth" -->
                <data
                    android:host="oauth"
                    android:scheme="@string/kakao_login_native_key_MANIFEST" />
            </intent-filter>
        </activity>




        <activity
            android:name=".views.splash.view.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>